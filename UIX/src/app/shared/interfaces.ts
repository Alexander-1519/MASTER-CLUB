export interface dataForNewUser {
  email: string;
  password: string;
  role: number;
  username: string;
  phoneNumber: number;
}
export interface dateForNewUserStepOne {
  email: string;
  role: number;
}
